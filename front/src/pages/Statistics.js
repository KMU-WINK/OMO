import React, { Component } from 'react';

import Header from "../components/common/header";
import Footer from "../components/common/footer";

const Statistics = (props) => {

    return (
        <div>
            <Header state={"Back"} title={"통계 페이지"}/>
                통계 페이지 테스트
            <Footer />
        </div>
    )
}

export default Statistics;
